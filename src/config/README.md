# Config

Centralized, environment-aware configuration for operational runtime behavior.

Configuration supports routes, execution settings, governance thresholds, screenshot settings, accessibility settings, retry settings, and report settings. All loaded configuration is validated with Zod before use so operational behavior can change without code edits.
