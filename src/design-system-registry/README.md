# Design System Registry

The design-system registry is the authoritative source of governance truth for components, variants, tokens, states, accessibility requirements, and ownership metadata.

It supports JSON loading, schema validation, semantic validation, version metadata, and deterministic governance contract generation. Governance contracts should derive from registry definitions whenever possible.
