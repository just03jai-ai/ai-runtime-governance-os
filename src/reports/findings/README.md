# Findings

Finding report builders live here.

The report engine consumes `VerifiedFinding[]` plus optional runtime evidence and produces structured operational JSON reports and static HTML summaries. It does not build an interactive dashboard.
