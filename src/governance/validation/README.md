# Governance Validation

Deterministic governance validation services live here.

The validation engine compares normalized `RuntimeEvidence` against approved JSON contracts and returns structured findings with severity, route, component, evidence, expected value, actual value, and confidence.
