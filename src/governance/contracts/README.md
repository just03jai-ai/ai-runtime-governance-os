# Governance Contracts

Shared TypeScript contracts for execution requests, runtime snapshots, telemetry events, runtime evidence, and governance policy contracts.

The `*.contract.json` files are deterministic design-system governance contracts. They are data, not code, so future tooling can load, review, version, and distribute approved rules independently from the validation engine.
