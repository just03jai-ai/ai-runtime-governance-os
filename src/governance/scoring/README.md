# Governance Scoring

`GovernanceScoringEngine` provides deterministic weighted scoring for operational governance quality.

It scores verified findings, route health, execution stability, historical governance trend, drift penalties, recurring regressions, component misuse, token drift, accessibility violations, and verification confidence. It does not use model calls or non-deterministic evaluation.
