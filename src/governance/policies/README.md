# Governance Policies

Policy descriptors define the deterministic rule categories supported by governance validation.

Policies must operate only on normalized runtime evidence. They must not infer intent, classify designs with AI, or inspect raw browser state directly.
