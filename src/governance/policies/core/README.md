# Core Governance Policy Pack

The core policy pack contains deterministic runtime UI governance rules. Each
policy reads normalized `RuntimeEvidence` and active governance contracts, then
returns evidence-backed findings without AI inference.

Policies included:

- token drift detection
- forbidden inline styles
- typography mismatch
- invalid component variants
- accessibility violations
- spacing inconsistency
- unauthorized components
