# Governance Agent

Deterministic policy engine for normalized runtime evidence.

`GovernanceAgent` loads governance contracts, applies validation policies, classifies severity through the governance utilities, and returns structured findings. It does not use LLMs and must not invent findings outside contract-backed validation.
