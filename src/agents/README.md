# Agents

Operational agent entry points live here.

Each agent emits structured operational logs through `src/shared/logger`, including start, completion, duration, correlation ID, route context when available, and failure reasons.
