# Analyzer Agent

Deterministic operational intelligence over verified findings.

`AnalyzerAgent` clusters findings, identifies recurring patterns, detects token drift and component misuse, summarizes route hotspots, and generates root-cause summaries without autonomous reasoning loops or LLM usage.
